import { View, StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  view: {
    height: "100%",
    width: "100%",
    backgroundColor: "#fff",
  },
  header: {
    display: "flex",
    flexDirection: "column",
  },
  closeButton: {
    width: 20,
    marginLeft: "auto",
    marginRight: 20,
  },
});
